org.gradle.jvmargs=-Xmx3G
org.gradle.daemon=true
org.gradle.parallel=true

# Minecraft / NeoForge
minecraft_version=1.21.1
minecraft_version_range=[1.21.1, 1.22)
neo_version=21.1.221
neo_version_range=[21.1.0,)
loader_version_range=[4,)

# Mod metadata
mod_id=customperm
mod_name=CustomPerm
mod_license=GPL-3.0-only
mod_version=1.0.4
mod_group_id=com.arcadia.customperm
mod_authors=THEFricadelle
mod_description=Granular server command permission system with optional LuckPerms alias integration.

# Optional Parchment mappings (comments-only, safe to keep)
parchment_minecraft_version=1.21
parchment_mappings_version=2024.11.10

# LuckPerms API (compileOnly)
luckperms_api_version=5.4

# TesseraUI (compileOnly, optional) — CurseForge file id 8116441 = TesseraUI 1.1
tesseraui_version=1.1

