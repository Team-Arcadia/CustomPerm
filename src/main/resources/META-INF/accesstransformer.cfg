# NeoForge Access Transformer for CustomPerm
#
# Format: <access>[-f] <fully.qualified.Class> <field|method[(desc)returnDesc]>
#   public-f  -> make public AND remove final
#
# We expose Brigadier's CommandNode#requirement so the CommandTreeRewriter
# can replace it directly, without reflection.

public-f com.mojang.brigadier.tree.CommandNode requirement
